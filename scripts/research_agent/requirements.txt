# research-agent dependencies
# Core — no external LLM API key needed, uses belt + OpenCode
belt>=0.1.0

# PDF generation (optional — auto-falls back to HTML if missing)
weasyprint>=62
markdown>=3.5
fpdf2>=2.7

# Dev / testing
pytest>=8
pytest-asyncio>=0.24
