version: '3.8'

services:
  agent:
    build:
      context: ..
      dockerfile: app/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - ENV=development
      - SECURITY_MODE=block
    volumes:
      - ..:/app
