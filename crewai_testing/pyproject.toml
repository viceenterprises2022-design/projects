[project]
name = "crewai_testing"
version = "0.1.0"
description = "crewai-testing using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[anthropic,google-genai,tools]==1.14.4",
]

[project.scripts]
crewai_testing = "crewai_testing.main:run"
run_crew = "crewai_testing.main:run"
train = "crewai_testing.main:train"
replay = "crewai_testing.main:replay"
test = "crewai_testing.main:test"
run_with_trigger = "crewai_testing.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
